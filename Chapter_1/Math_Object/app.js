const num1 = 100;
const num2 = 50;


//simple math with numbers
val = num1 + num2;
val = num1 - num2;
val = num1 * num2;
val = num1 / num2;
val = num1 % num2;

console.log(val);


//Math Object
val = Math.PI;
val = Math.E;
val = Math.round(2.8); //round up -> 3
val = Math.ceil(2.8); //round up
val = Math.floor(2.8); //round down -> 2
val = Math.sqrt(16); //8
val = Math.abs(-14);
val = Math.pow(2, 4); //16
val = Math.max(2, 1 , 0, -1); //2
val = Math.min(9, 0, 2, 4); //0
val = Math.random(); //returns a decimal
val = Math.floor(Math.random() * 20 + 1); // 20 is exclusive so need to add 1

